# Mở lại quyền truy cập cho public/ (bị chặn mặc định bởi .htaccess ở gốc repo).
Options -Indexes

<IfModule mod_authz_core.c>
  Require all granted
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Allow from all
</IfModule>
