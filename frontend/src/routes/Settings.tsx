export default function Settings() {
  return (
    <div className="card shadow-sm">
      <div className="card-body">Cấu hình (ADMIN) sẽ gồm lớp, giáo viên, lý do, quà, nhật ký.</div>
    </div>
  );
}
